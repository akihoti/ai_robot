# AI Robot TODO

> 维护规则：
> - 已完成项用 `[x]`
> - 未完成项用 `[ ]`
> - 后续每次实现一项，都同步更新这个文件

## 已完成基础能力

- [x] 服务端统一网关部署完成
- [x] 服务端 HTTP 健康检查可用
- [x] 服务端 WebSocket 会话入口可用
- [x] 服务端 Xinference ASR / TTS 联通
- [x] 服务端 RAGFlow 联通
- [x] 服务端欢迎语 WebSocket 返回可用
- [x] 服务端 LLM 真流式输出已验证
- [x] 服务端分段 TTS 回传已验证
- [x] 边缘端视觉检测触发欢迎事件
- [x] 边缘端 VAD 截句并发送 utterance
- [x] 边缘端播放队列支持多段 TTS 连续播放
- [x] 边缘端基础视线跟踪 / 云台追踪已接入

## 当前主线目标

- [ ] 完成边缘端“唤醒 -> 对话 -> 关注 -> 恢复待机”的完整状态机
- [ ] 完成连续多轮对话
- [ ] 完成会话期间的稳定视线关注
- [ ] 完成说话/听话/动作三者之间的互斥和仲裁

## 优先级原则

- `P0`：先补齐系统可稳定运行的基础能力
- `P1`：系统能跑稳之后，再补完整交互能力
- `P2`：最后做体验增强和鲁棒性优化

## 当前建议执行顺序

- [x] Phase 1：先完成会话状态机、连续监听、基础恢复，确保系统能稳定运行
- [ ] Phase 2：再完成主目标锁定、稳定视线关注、会话完整性
- [ ] Phase 3：最后完成打断、多唤醒、动作仲裁和参数化优化

## P0 会话状态机与基础运行

- [x] 设计统一会话状态枚举：`idle / tracking / welcoming / listening / thinking / speaking / followup_listening / disengaged`
- [x] 增加边缘端会话控制器，统一管理视觉、语音、播放、动作状态
- [x] 明确每个状态的进入条件、退出条件、超时条件
- [x] 接入欢迎语结束后的自动监听状态切换
- [x] 接入回答播放结束后的 follow-up 监听状态切换
- [x] 接入无人目标消失后的状态回退逻辑

## P0 连续对话

- [x] 增加 `session_id` 和边缘端当前会话上下文
- [x] 支持回答结束后短时继续监听，而不是回到单轮模式
- [x] 支持连续多轮对话超时退出
- [x] 支持连续多轮中不重复欢迎
- [x] 补充服务端会话上下文传递字段

## P1 注意力 / 视线关注

- [x] 增加“当前主目标用户”锁定机制
- [x] 会话进行中优先跟踪主目标，不随新目标立即切换
- [x] 增加目标短时丢失的容忍窗口
- [x] 增加 speaking / listening / thinking 各状态下不同的视线策略
- [x] 增加会话结束后的云台回中/待机策略

## P0 音频交互质量

- [x] 增加 speaking 期间的麦克风抑制策略
- [x] 避免机器人自身 TTS 被再次录入 VAD
- [ ] 验证多段 TTS 连播是否平滑，必要时增加缓冲
- [x] 增加 TTS 播放失败后的恢复逻辑

## P2 打断与仲裁

- [x] 支持用户在机器人 speaking 时插话打断
- [x] 支持打断后立即停播并切换到 listening
- [x] 定义动作、语音播放、云台跟踪之间的优先级
- [x] 增加高优先级动作对普通动作的打断机制

## P2 唤醒机制

- [x] 明确视觉唤醒正式策略
- [x] 明确语音唤醒词是否为必选能力
- [ ] 如果需要，接入真实 wake word engine
- [x] 将视觉唤醒与语音唤醒统一纳入状态机

## P0 稳定性与恢复

- [x] 增加 WebSocket 会话失败后的自动恢复流程
- [x] 增加 ASR / LLM / TTS 任一失败后的会话恢复逻辑
- [x] 增加云台控制异常时的降级模式
- [x] 增加边缘端关键状态和错误日志埋点

## P2 配置产品化

- [x] 增加 `followup_listen_timeout_ms`
- [x] 增加 `target_lock_timeout_ms`
- [x] 增加 `speech_interrupt_enabled`
- [x] 增加 `suppress_mic_while_speaking`
- [x] 增加 `idle_return_to_center_seconds`
- [x] 增加 `welcome_once_per_session`

## 联调与验收

- [ ] 验证欢迎语 -> 监听 -> 单轮问答闭环
- [ ] 验证连续两轮以上对话闭环
- [ ] 验证会话期间稳定跟人，不乱切目标
- [ ] 验证 speaking 时用户插话打断
- [ ] 验证目标离开后的回退逻辑
- [ ] 验证异常情况下系统可恢复到可交互状态
